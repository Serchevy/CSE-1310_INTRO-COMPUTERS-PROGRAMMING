#include <stdio.h>

int main(void) 
{
     printf ("Servando Olvera\n1001909287\n");
     printf ("-------------------\n");
     
     // Dimon shaped with asterisks 
     {
     printf ("*\n**\n***\n****\n*****\n ****\n  ***\n   **\n    *\n");
     printf ("-------------------\n");
     }
     
     /* Initials "S" and "O" shaped with their respective letter. Ion't knwo if using this many spaces was the way to do it, but it works. */
     {
     printf ("SSSSSSSSSSSS    OOOOOOOOOO \nSSS            O          O\nSSS            O          O\nSSSSSSSSSSSS   O          O\n         SSS   O          O\n         SSS   O          O\nSSSSSSSSSSSS    OOOOOOOOOO \n");
     printf ("-------------------\n");
     }
     
     /* Same as before, don't knwo if using this many spaces was the way to do it, but it works. Also, the backslash, for me at least, wasn't that tilted sp the tree ended up looking a bit weird. */
     {
     printf ("     /\\\n    /  \\\n   /  ' \\\n  /  \"   \\\n /  \"   * \\\n/ %%    '   \\\n------------\n    ||||\n    ||||\n    ||||\n    ||||\n");
     }
     return 0;
}
